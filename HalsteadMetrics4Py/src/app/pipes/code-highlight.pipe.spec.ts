import { CodeHighlightPipe } from './code-highlight.pipe';

describe('CodeHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
